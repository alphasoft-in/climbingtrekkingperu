---
import { allToursData } from '@data/tours/toursData';
import PackageTemplate from '@components/PackageTemplate.astro';

export async function getStaticPaths() {
  const boliviaTours = Object.entries(allToursData).filter(([_, data]) => data.subCategory === 'bolivia');
  
  return boliviaTours.map(([slug, data]) => ({
    params: { slug },
    props: { data }
  }));
}

const { data } = Astro.props;
const lang = 'es';
---

<PackageTemplate lang={lang} data={data} />
