---
import { allToursData } from '@data/tours/toursData';
import PackageTemplate from '@components/PackageTemplate.astro';

export async function getStaticPaths() {
  const ecuadorTours = Object.entries(allToursData).filter(([_, data]) => data.subCategory === 'ecuador');
  
  return ecuadorTours.map(([slug, data]) => ({
    params: { slug },
    props: { data }
  }));
}

const { data } = Astro.props;
const lang = 'en';
---

<PackageTemplate lang={lang} data={data} />
