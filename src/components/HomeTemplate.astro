---
import Layout from '@layouts/Layout.astro';
import Slider from './Slider.astro';
import Institutional from './Institutional';
import WorkArea from './WorkArea';
import TeamSection from './TeamSection';
import Gastronomy from './Gastronomy';
import Logistics from './Logistics.astro';
import EthicsBento from './EthicsBento.astro';
import SuggestedRoutes from './SuggestedRoutes.astro';
import { useTranslations } from '@i18n/utils';

interface Props {
  lang: 'es' | 'en';
  data: any;
}

const { lang, data } = Astro.props;
const t = useTranslations(lang);
---

<Layout 
	title={data.title} 
	description={data.description}
>
	<Slider lang={lang} slides={data.slides} buttonLabel={t('slider.btn')} />
	<Institutional data={data.institutional} />
	<WorkArea data={data.workArea} />
	<TeamSection data={data.team} />
	<Gastronomy data={data.gastronomy} />
	<Logistics data={data.logistics} />
	<EthicsBento data={data.ethics} />
	<SuggestedRoutes lang={lang} />
</Layout>
